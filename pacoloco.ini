[hub]
upstream = http://mirrors.kernel.org/archlinux
peer_timeout = 2  # timeout for peer reply, if peer did not reply takes more than this limit then peer is considered dead. Default - 1 second.
[peer]
foomachine = repo/db,repo/pkg
